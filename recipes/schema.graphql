schema {
    query: Query
}

type RecipeItem {
    itemName: String
    amount: int
}

type NutrientToAmount {
    nutrient: String
    amount: int
}

type Recipe {
    recipeName: String
    ingredients: [RecipeItem]
    calories: int
    nutrients: [NutrientToAmount]
}

type Recipes {
    recipes: Recipe
}

type RecipesResult {
    success: Boolean!
    errors: [String]
    results: [Recipes]
}

type Query {
    recipes: RecipesResult
}